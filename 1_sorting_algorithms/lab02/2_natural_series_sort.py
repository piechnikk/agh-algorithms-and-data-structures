# natural series sort (piece of list that sorted by default)

# merge(L1,L2)->L3 merge two sorted linked lists

# getns(L1)->L2 cut first natural series

# concat(L1,L2)->L move to the end

# empty(L1)->T/F


# def sort(L):
#     while not empty(L):
#         L1 = getns(L)
#         L2 = getns(L)
#         new_list = merge(L1, L2)
#         concat(L, new_list)
